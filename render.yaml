services:
  - type: web
    name: pdftranslate
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
    healthCheckPath: /
    autoDeploy: true
